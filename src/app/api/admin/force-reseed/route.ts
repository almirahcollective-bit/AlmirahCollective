import { NextResponse } from " next/server\; import { db } from \@/db\; import { products, categories, orderItems, orders, reviews, complaints, leads, newsletterSubscribers, pressMentions, addresses, customers } from \@/db/schema\; import { CATEGORIES, PRODUCTS } from \@/lib/catalog\; export async function GET() { try { await db.delete(orderItems); await db.delete(orders); await db.delete(reviews); await db.delete(complaints); await db.delete(leads); await db.delete(newsletterSubscribers); await db.delete(pressMentions); await db.delete(addresses); await db.delete(customers); await db.delete(products); await db.delete(categories); await db.insert(categories).values(CATEGORIES.map((c, i) => ({ slug: c.slug, name: c.name, description: c.description, imageUrl: c.image, sortOrder: i }))); await db.insert(products).values(PRODUCTS.map(p => ({ slug: p.slug, name: p.name, description: p.description, shortDescription: p.shortDescription, categorySlug: p.categorySlug, price: String(p.price), compareAtPrice: p.compareAtPrice ? String(p.compareAtPrice) : null, material: p.material, careInstructions: p.careInstructions, images: p.images, colors: p.colors, sizes: p.sizes, tags: p.tags, stock: p.stock, isFeatured: p.isFeatured, isTrending: p.isTrending, rating: p.rating, reviewCount: p.reviewCount }))); return NextResponse.json({ success: true, message: \Reseeded successfully!\ }); } catch (e: any) { return NextResponse.json({ success: false, error: e.message }); } }
